/**
 * Re-export from src/ — pure types live in src/lib/runway/gantt/types.ts.
 * This shim keeps the CLI entry (scripts/runway-gantt.tsx) import paths unchanged.
 */
export type * from "../../../src/lib/runway/gantt/types";
